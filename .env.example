# =============================================================================
# Rust TCP智能转发器 - 环境变量配置示例
# =============================================================================
# 
# 复制此文件为 .env 并根据需要修改配置
# cp .env.example .env

# 日志配置
RUST_LOG=info
# 可选值: trace, debug, info, warn, error

# 配置文件路径
CONFIG_PATH=/app/config/config.yaml

# 服务端口（如需覆盖配置文件中的设置）
# TCP_LISTEN_PORT=1234
# METRICS_PORT=9099

# Docker相关
DOCKER_IMAGE_TAG=tcp-forwarder:latest
CONTAINER_NAME=tcp-forwarder

# 资源限制
MEMORY_LIMIT=512m
CPU_LIMIT=0.5

# 监控配置（用于完整部署）
PROMETHEUS_PORT=9090
GRAFANA_PORT=3000
GRAFANA_ADMIN_PASSWORD=admin

# 网络配置
NETWORK_NAME=tcp-forwarder-net

# 存储配置
LOG_VOLUME=./logs
CONFIG_VOLUME=./tcp-forwarder/config.yaml
IP_LIST_VOLUME=./tcp-forwarder/ip_list.txt

# 健康检查配置
HEALTH_CHECK_INTERVAL=30s
HEALTH_CHECK_TIMEOUT=10s
HEALTH_CHECK_RETRIES=3
HEALTH_CHECK_START_PERIOD=10s
